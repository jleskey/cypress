/**
 * @file jio.h
 * @author Joseph Leskey
 */

#ifndef J_IO_H
#define J_IO_H

/**
 * Prints a formatted string n times.
 *
 * @param[in] format The format string
 * @param[in] times Number of times to repeat
 * @param[in] ... Parameters
 */
void jprintn(const char *format, const int times, ...);

#endif // J_IO_H
